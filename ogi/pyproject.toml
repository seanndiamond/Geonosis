[project]
name = "ogi-court"
version = "0.2.0"
description = "Court-governed epistemic research kernel and provenance benchmark"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
